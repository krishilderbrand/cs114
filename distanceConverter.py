mi = float(km / 1.60934)
mi = float(ft / 5280)
mi = float(m / 1609.34)
km = float(mi * 1.60934)
km = float(km / 3280.84)
km = float(m / 1000)
ft = float(mi * 5280)
ft =
m = float(km * 1000)



distance = float(input('Please enter the distance.   '))
units = input('Please input the unit of measurement (mi, km, ft, m).   ')
targetUnits = ('Please input the measurement you would like it converted to.   ')
